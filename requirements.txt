# Обязательные зависимости
cloudscraper
requests
Pillow

# Необязательные (но настоятельно рекомендуемые), только для настоящего
# шифрования сохранённых API-ключей/User ID через OS keychain + Fernet.
# Без них приложение работает, но откатывается на обычное кодирование
# base64 для учётных данных (см. README - "Encrypted credential storage").
keyring
cryptography

# Примечание: tkinter - часть стандартной библиотеки Python и не
# устанавливается через pip (на некоторых Linux-дистрибутивах может
# требовать отдельного системного пакета, например python3-tk).
